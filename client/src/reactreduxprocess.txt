// provides makes the redux store accessible to any components or nested components

// Redux Thunk is a popular middleware for React Redux, works as a action creator.
// react redux: It allows React components to read data from a Redux Store, and dispatch Actions to the Store to update data
// createstore: store An object that holds the complete state of your app.
   